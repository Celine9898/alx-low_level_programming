#include <stdio .h>

/**
 * main - Entry piont
 *
 * Return: Always 0 (when successfully printed the function)
 */
int main(viod)
{puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
